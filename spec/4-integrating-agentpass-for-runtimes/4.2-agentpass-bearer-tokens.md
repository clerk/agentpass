---
nav_title: Request a Bearer Token
---

# 4.2. Request a AgentPass Bearer Token

This section defines the Runtime flow for AgentPass Bearer Tokens.

Runtime requests `type=service_bearer_token` and receives an access token for API access.

1. Discover Service Provider support.
2. Call `POST {service_e}/resolve-deployments` with `request.delegating_principal_email`.
3. Require `response.delegating_principal_domain` and one of:
   - `response.authoritative_deployment`, or
   - `response.trusted_federated_deployments`.
4. If `authoritative_deployment` is present, use it.
5. If `trusted_federated_deployments` is present, choose one deployment from that list.
6. POST `{deployment_e}/requests` with `type=service_bearer_token` and returned `delegating_principal_domain` (where `deployment_e` is from the selected deployment returned by `POST {service_e}/resolve-deployments`).
8. Poll until approved.
9. Receive `access_token` and `token_type` (`Bearer`) (optional `cnf`).
10. Call Service Provider with `Authorization: Bearer {access_token}`.

Runtime MUST NOT perform direct authoritative discovery as part of this flow.
