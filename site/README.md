# AgentPass Site

TanStack Start app that renders markdown from `../spec`.

## Local dev

```bash
npm install
npm run dev
```

## Deploy (Vercel)

Set the Vercel Project **Root Directory** to `site`.

Nitro is configured in `vite.config.ts` via `nitro()`.
